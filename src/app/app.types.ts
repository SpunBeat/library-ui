export type CounterPayload = {
    counterHijo: number, 
    index: number
};

export type Color = {
    name: string;
    index: number;
    counter: number;
};

export type Author = {
    Author_ID: number;
    firstName: string;
    lastName: string;
}